# Verification and smoke tests

Test theo lớp: `test_ai_env.py` kiểm tra env AI; `test_dds_*` và script Wi-Fi
kiểm tra DDS; `test_unitree_dds_*` kiểm tra SDK. Không dùng test này thay thế
SOP/safety gate cho hardware run.
