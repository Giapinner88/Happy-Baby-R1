# MuJoCo DDS bridge entry points

- `run_r1_unitree_mujoco_bridge.py`: chạy bridge R1 không policy để kiểm tra
  asset/DDS semantics.
- `run_unitree_mujoco_policy.py`: chạy bridge và ONNX policy cùng domain.

Đây là lớp parity trước hardware, không phải lệnh điều khiển robot thật.
