# Legacy policy workspace

Thư mục này được giữ để tương thích với thử nghiệm cũ. Policy phát hành mới và
model runtime phải dùng `data/policies/` và `data/models/` để có phân tách rõ
giữa artefact theo run và model đang active.
