# Active runtime models

Chỉ đặt model được runtime tham chiếu trực tiếp ở đây. Với R1 MuJoCo, kiểm tra
`readlink -f data/models/unitree_mujoco_policy/r1_velocity.onnx` trước mỗi lần
bridge parity hoặc hardware gate.
