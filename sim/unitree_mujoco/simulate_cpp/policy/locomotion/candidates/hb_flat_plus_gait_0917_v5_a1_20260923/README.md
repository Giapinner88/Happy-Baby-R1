# HB Flat-Plus Gait 0917 V5 A1 candidate

Imported byte-for-byte from `HB/high_level_2/policies/locomotion/flat_plus/policy_flat_plus_gait_0917_v5_a1.onnx` for isolated C++ MuJoCo evaluation. This package does not alter `config/tuning.yaml` or select the candidate by default.

Contract: `flat_plus_gait_h4_v1`, 335 observations, 24 actions. Use gait period 0.6 s and settle dwell 0.5 s from the matching HB example profile. The staged model checksum is recorded in `SHA256SUMS`.
