# Collected MJLab artifacts

`collect` creates one new artifact directory here per source run. These files
are not approved runtime policies. They remain tied to their source checkpoint
until `promote` verifies a compatible evaluation manifest and writes a new
destination under `data/policies/` or `data/models/`.
