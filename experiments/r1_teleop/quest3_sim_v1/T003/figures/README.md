# T003 figures

This directory owns durable, reproducibly generated T003 summary figures. Raw
trajectory CSV, video and per-run plots remain inside `../runs/<run-id>/` so a
figure always points back to its immutable execution evidence.

