# Immutable T003 runs

Each direct child is one immutable T003 execution. The active split and legacy
selection are recorded in [`../metadata/evidence_catalog.json`](../metadata/evidence_catalog.json).

