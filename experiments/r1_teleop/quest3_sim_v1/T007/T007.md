# T007 — Live/replay equivalence and arm/head simulation pilot

## Cong toc do KHONG phai cong that — 2026-08-23 (ket qua cuoi ngay)

Tach chi phi vong dieu khien do trong Isaac:

```
buoc vong dieu khien (trong Isaac) : 37.1 ms
CHI bo giai IK (do ngoai Isaac)    :  6.3 ms
phan con lai (PhysX + render)      : 30.8 ms   <- KHONG ton tai tren phan cung
```

Tran nhip do **bo giai** la **159 Hz** o 20 vong lap, khong phai 27 Hz do duoc
trong Isaac. Con so 26.97 Hz bi gioi han boi **mo phong**, khong phai bo dieu
khien. Vi vay viec ha `max_iterations` tu 40 xuong 20 truoc do la **danh doi
chat luong khong can thiet**.

**So hoi tu khong doi theo ngan sach lap** — 221/400 o ca 20, 40, 60 va 100 vong
— vi phan khong hoi tu la bay gioi han khop, khong phai thieu vong lap. Nhung
nghiem *projected* van tot len dan.

Do trong Isaac that (`--no-video` de bot phan mo phong):

| max_iter | Hz (Isaac) | dispatch | hoi tu | p95 T/P | max T/P |
|---|---:|---:|---:|---:|---:|
| 20 | 26.97 | 1103 | 35% | 181.7 / 206.0 mm | 253.7 / 418.2 mm |
| **100** | 14.47 | 593 | 58% | **143.9 / 165.3 mm** | **204.0** / 418.4 mm |

O 100 vong: **p95 trai 143.9 mm dat nguong 150 mm**, phai 165.3 mm van vuot
1.1x; max trai 204.0 mm dat nguong 250 mm, phai 418.4 mm van vuot.

Chi phi thuan bo giai o 100 vong la **26.8 ms**, van duoi ngan sach phan cung
33.3 ms. Nhip 14.47 Hz trong Isaac la do PhysX, khong phai do bo giai.

**Trang thai cuoi ngay:**

| | dau ngay | cuoi ngay | nguong |
|---|---:|---:|---:|
| chi phi bo giai | 291 ms | **6.3-26.8 ms** | 33.3 ms — DAT |
| p95 trai | — | 143.9 mm | 150 mm — DAT |
| p95 phai | — | 165.3 mm | 150 mm — chua |
| max trai | — | 204.0 mm | 250 mm — DAT |
| max phai | — | 418.4 mm | 250 mm — chua |

Tay phai van la ben yeu hon o ca hai chi so. Phan du nam o bay gioi han khop ma
seed restart moi xu ly duoc mot phan.

## Quet toc do sim-online: 3.46 -> 27.01 Hz — 2026-08-23

Chay tuan tu (song song lam hong phep do nhip vi tranh GPU):

| cau hinh | Hz | ms/IK | hoi tu | wrist p95 T/P |
|---|---:|---:|---:|---:|
| 40 iter + restart | 19.99 | 50.1 | 50% | 161.1 / 193.0 mm |
| **20 iter + restart** | **27.01** | 37.1 | 35% | **180.5 / 214.0 mm** |
| 20 iter, khong restart | 28.71 | 34.9 | 26% | **741.0** / 318.4 mm |
| 12 iter, khong restart | 30.02 | 33.3 | 11% | 777.6 / 313.3 mm |

**Seed restart khong duoc bo.** No chi ton 2 ms nhung bo di thi p95 vot tu 180
len 741 mm — gap 4 lan — vi canh tay ket o gioi han khop dung nhu bay da phan
tich o tren. Day la vi du ro rang ve toi uu sai cho: tiet kiem thoi gian o dung
cho dat nhat.

`12 iter` dat 30.02 Hz nhung chi **11% hoi tu**. Nhanh ma khong giai duoc thi
khong phai bo dieu khien dung duoc; con so Hz mot minh khong noi len dieu gi.

Profile live chot o **20 iter + restart + Jacobian giai tich**.

**Trang thai so voi muc tieu online:**

| | dat duoc | nguong | con cach |
|---|---:|---:|---|
| nhip dieu khien | 27.01 Hz | 30 Hz | 1.11x |
| wrist p95 | 180.5 / 214.0 mm | 150 mm | 1.2-1.4x |

Tu dau ngay: nhip 3.46 -> 27.01 Hz (**7.8 lan**), khoang cach tu 8.7x xuong
1.11x. Chat luong van chua dat nguong.

## Jacobian giai tich: sim-online tu 3.46 len 19.99 Hz — 2026-08-23

`upper_body_task_jacobian` dung sai phan trung tam: 2*dof lan danh gia sai so
nhiem vu, moi lan mot FK toan than, tuc 26 FK moi vong lap. Do duoc **4.654 ms**
tren mot vong 5.36 ms — 96% chi phi.

`upper_body_analytic_jacobian` thay bang cong thuc hinh hoc dong: mot luot FK
cung cap moi khung can thiet. Voi khop quay co truc `a` va goc `o`, endpoint
chay theo `a x (p - o)` va vat the quay theo `a`; hang huong dung dong nhat thuc
goc nho `d/dq Log(R* R^T) = -a`, chinh xac tai residual bang 0 va bac nhat o lan
can. Huong co tay von la khop tot nhat co trong so, khong bao gio gate viec chap
nhan, nen xap xi nay khong lam mat gi ma phuong phap tuyen bo.

**Kiem chung:** khop voi ban sai phan toi `1.8e-10` tren ca ba body mode va ca
hai asset. Cot than duoc dung khung tuong minh — asset mo phong co goc waist_roll
dung bang identity nen mot loi tat o do se DUNG DO MAY; chi asset vendor moi lo
ra dieu do, va test kiem ca hai.

**Toc do:** 4.654 ms -> **0.410 ms**, nhanh **11.4 lan**.

Chay lai sim-online tren SEG2, chi doi Jacobian:

| | Hz | ms moi buoc IK | dispatch |
|---|---:|---:|---:|
| sai phan | 3.46 | 291.2 | 142 / 3666 |
| **giai tich** | **19.99** | **50.1** | **818 / 3666** |

Nhanh **5.8 lan**, dispatch tang **5.8 lan**. Con cach nguong 30 Hz **1.5 lan**
(50.1 ms so voi 33.3 ms) — tu 8.7 lan xuong 1.5 lan.

Bo test teleop chay tu ~20 s xuong 6.7 s vi moi solve trong test cung nhanh len.

Cong toc do trong `hardware_gate.md` **van chua dong**, nhung khoang cach da tu
"khong kha thi" thanh "con mot buoc".

## Sim-online voi bo giai phan cung: KHONG DAT — 2026-08-23

`t007_simonline_seg2_20260823` chay dung `WholeUpperBodyIsaacLabSink`, tuc bo
giai ma `run_r1_quest3_hardware_targets.py` dung, voi trace ghi san thay Quest
live, o toc do that trong Isaac:

```
achieved_control_hz : 3.46 Hz  /  30 Hz yeu cau       cham 8.7 lan
moi buoc co IK      : 291 ms   /  ngan sach 33 ms     vuot 8.8 lan
dispatch            : 142 / 3666 lenh nhan (3.9%)
so vong lap IK      : mean 29.5, max 40 (cham tran)
trang thai IK       : 46 converged, 93 iteration_budget_exhausted, 3 projected
```

Bo giai cham tran 40 vong lien tuc vi phan lon target khong giai duoc chinh xac.

**Ba bo giai, duong nhanh nhat khong phai duong phan cung:**

| bo giai | toc do | phan cung dung? |
|---|---:|---|
| coupled DLS (`whole_upper_body.py`) | 3.5-10 Hz | CO |
| offline continuation | 0.19x realtime | khong |
| causal tracking (`solve_r1_t007_causal_tracking.py`) | 18-22 Hz | khong |

Toan bo cong viec ngay 2026-08-23 (bo rang buoc khuyu, giu hong, gioi han
9.0/133, duong nhan qua, seed restart) lam tren `offline_continuation.py`, KHONG
phai bo giai nam tren duong phan cung. Ket qua do khong chuyen sang phan cung
duoc neu khong port.

Quyet dinh: **khong dua xuong phan cung** cho toi khi sim-online chay duoc. Day
la mot cong CHUA DONG trong `hardware/teleop/docs/hardware_gate.md`.

Nut that da xac dinh: `upper_body_task_jacobian` dung sai phan trung tam, 26 lan
FK moi vong lap, chiem ~96% chi phi mot vong (5.163 ms tren 5.36 ms do duoc).

## Duong offline la CAN TREN LY THUYET, khong phai du bao online — 2026-08-23

Muc tieu cua cong viec offline la tien toi teleop truc tuyen on dinh. Doi chieu
voi muc tieu do, pipeline offline hien tai **khong chuyen sang online duoc**, va
ket qua cua no khong duoc doc nhu nang luc he thong.

Bon thanh phan mang tinh khong nhan qua:

| thanh phan | ban chat |
|---|---|
| `anchor_policy: maximum_bilateral_lateral_hand_separation` | quet toan trace chon mot moc neo roi lan ra hai phia |
| `refinement_sweeps` | co luot quet **nguoc** `range(count-2, 0, -1)` |
| `manifold_reprojection_sweeps` | quet lai ca duong, co chieu nguoc |
| `solver_stride: 4` + noi suy | can node **tuong lai** de noi suy |

Va ngan sach thoi gian: **356 ms moi mau song phuong** (realtime factor 0.187),
cham hon **11 lan** so voi 33.3 ms ma online 30 Hz doi hoi.

Do do moi con so tu duong offline — ke ca p95 103 mm dat duoc sau khi nang gioi
han dong hoc — la gioi han tren khi biet truoc moi thu, khong phai thu online
se dat duoc.

## Duong nhan qua song song va cai gia cua tinh nhan qua — 2026-08-23

`scripts/teleop/solve_r1_t007_causal_tracking.py` chay mot luot nghiem ngat theo
thoi gian: warm start la lenh **da gui** o mau truoc, so hang van toc chi thay
mau truoc, so hang gia toc bi bo hoan toan vi no can mau **sau**, khong anchor,
khong multistart, khong sweep nguoc, khong reprojection, giai moi mau o tan so
nguon. Lam muot dau ra dung co che cua upstream: `WeightedMovingFilter([0.4,
0.3, 0.2, 0.1])` ap len moi nghiem — hoi tu hay khong — truoc khi no thanh ca
lenh lan warm start ke tiep.

Do tren `t007_whole_upper_body_20260823T072635Z`:

| | offline | nhan qua | chenh |
|---|---:|---:|---:|
| SEG1 p95 T/P | 95.7 / 100.6 mm | 107.1 / 100.5 mm | +11.3 / -0.1 |
| SEG1 max T/P | 119.2 / 121.0 mm | 140.0 / 121.6 mm | +20.8 / +0.6 |
| SEG2 p95 T/P | 103.3 / 107.7 mm | 128.5 / 196.1 mm | +25.2 / +88.4 |
| SEG2 max T/P | 881.7 / 802.0 mm | **499.1 / 327.6 mm** | **-382.7 / -474.5** |

Hai ket luan:

1. **SEG1 gan nhu khong mat gi.** Chuyen dong co giu tu the, toc do vua phai thi
   nhan qua bam ngang offline.
2. **Nhan qua TOT HON offline o peak.** Bo loc FIR cat doi cu nhay lon nhat
   (21.6 do -> 10.4 do), va vi nghiem **da loc** tro thanh warm start nen vong
   lap khong bao gio khoi hanh tu mot cu nhay nguyen ven. Duong offline khong co
   co che nay — no tin vao phat mem `velocity_weight` 0.03 va
   `acceleration_weight` 0.0005 so voi `position_weight` 20, tuc yeu hon hang
   nghin lan — nen 51 cu nhay 38-40 do song sot va limiter khuech dai thanh
   800+ mm.

Toc do nhan qua: **72 ms moi mau song phuong** (realtime factor 0.46). Nhanh hon
offline 5 lan nhung van cham 2.2 lan so vi nguong online 30 Hz.

## Bien an toan cho rang buoc khuyu — 2026-08-23 (v26/v27 va quet)

`_elbow_branch_residual` nay phat `max(0, pole_x + margin)` thay vi
`max(0, pole_x)`. `elbow_direction_margin_m = 0.0` tai lap hanh vi cu.

**Bien sua dung van de da chan doan.** Don dong tai bien tan ngay o 3 mm, va o
10 mm thi chinh sach khuyu song sot qua physics gan nhu hoan hao:

| cau hinh | don dong `abs(x)<0.5mm` T/P | wrist p95 T/P | forward off -> PhysX (T) | #fail |
|---|---:|---:|---|---:|
| 0 mm, w500 | **279 / 360** | 65.9 / 65.9 mm | 2.29% -> **17.92%** (15.6pp) | 2 |
| 3 mm, w500 | 4 / 3 | 113.3 / 91.7 mm | chua do PhysX | 2 |
| 6 mm, w500 | 1 / 1 | 132.7 / 103.2 mm | chua do PhysX | 4 |
| 10 mm, w500 | 2 / 4 | 156.4 / 123.7 mm | 0.86% -> **0.82%** (0.04pp) | 7 |
| **6 mm, w100** | 11 / 8 | **41.8 / 59.7 mm** | 1.58% -> 8.73% (7.2pp) | **2** |

**Danh doi that va rat doc.** Voi trong so khuyu giu nguyen 500, moi milimet
bien phai tra bang do chinh xac vi tri: p95 tang tu 65.9 len 156.4 mm o 10 mm,
va sinh them 5 acceptance check fail. Nguyen nhan la mat can bang trong so:
phat khuyu 500 so voi vi tri 20, gap 25 lan. Khong co bien thi so hang khuyu
gan nhu luon tat; co bien thi no bat tren hau het mau va chiem quyen ham muc
tieu. Su suy giam xuat hien o **ca mau trong tam** (+26 den +52 mm p95), nen
day khong phai do het bac tu do du.

`10 mm` bi `6 mm` chi phoi: cung muc khuyu nhung nhieu fail hon.

Ha trong so xuong 100 kem bien 6 mm cho vi tri **tot hon ca baseline**
(41.8/59.7 so voi 65.9/65.9 mm) va giu #fail o 2, nhung chi dong duoc mot phan
khoang cach PhysX: 7.15pp ben trai, 0.35pp ben phai. Bat doi xung trai/phai o
cau hinh nay chua duoc giai thich.

**Chua chot gia tri cho config chinh thuc.** Config hien dang de
`elbow_direction_margin_m = 0.010` va `elbow_forward_up_weight = 500`, tuc cau
hinh co #fail cao nhat. Can quyet dinh dua tren muc tieu: neu uu tien do ben
cua chinh sach khuyu thi 10 mm/w500; neu uu tien bam vi tri thi 6 mm/w100.
Chua co ca hai cung luc.

## Ràng buộc khuỷu không có biên an toàn — 2026-08-23 (v23b/v25)

Nới ngưỡng straightness từ vách cứng 165 độ sang dốc tuyến tính +-10 do da
**phoi ra** mot khiem khuyet phuong phap ma nguong cung dang che: phan so
`forward` cua offline va PhysX chenh nhau 15.6-20.5 diem phan tram, va chieu
chenh **dao nguoc** giua hai dinh nghia (nguong cung: PhysX 0.63% < offline
2.34%; doc mem: PhysX 17.92% > offline 2.29%). Dao chieu nhu vay khong the la
hien tuong vat ly.

Nguyen nhan: `_elbow_branch_residual` phat mot phia
`elbow_forward_up_weight * max(0, pole_x)` voi trong so 500 va **khong co bien
an toan**. Bo giai vi the day `pole_x` toi dung bien rang buoc thay vi vao sau
trong khu vuc tu nhien. Histogram xac nhan don dong:

| pole_x | offline | PhysX |
|---|---:|---:|
| `abs(x) < 0.5 mm` | **279 / 360** mau | 1 / 2 |
| `+5 .. +10 mm` | 4 / 8 | **343 / 342** |

279 mau trai va 360 mau phai nam **dung tai `pole_x` = 0**, voi `abs(pole)`
trung binh 33-37 mm nen chung khong bi loc boi nguong 10 mm. Trong PhysX ca
khoi do dich **dong bo** sang `+7.5 mm`, chi vi sai so bam khop elbow khoang
**2 do** (90.22 -> 88.50 do). Nguong phan loai la +5 mm, nen mot phan tu so mau
lat trang thai.

Ket luan: tuan thu khu vuc khuyu cua nghiem offline la **tuan thu bien**, dat
duoc voi bien an toan bang khong tren khoang 20-25% so mau. No khong song sot
qua physics, va se khong song sot qua phan cung. Tuyen bo "tu the robot tu
nhien" trong `REPORT_20260822.md` chi dung cho nghiem offline, khong dung cho
trang thai robot quan sat duoc.

Huong sua da xac dinh nhung **chua thuc hien**: cho rang buoc mot bien am, tuc
phat `max(0, pole_x + margin)` thay vi `max(0, pole_x)`, de nghiem nam sau
trong khu vuc tu nhien thay vi tren biên. Can chay lai va do lai sau khi sua.

## Giu hong va bo nguong cung — 2026-08-23 (v24/v25)

`hold_uncontrolled_waist_joints` da duoc them vao config, sink va runner.
**Lan cai dau la no-op**: chi ghi target 0 moi buoc, trong khi target waist von
da la 0 tu startup, nen do vong trung khit v21 (22.38 do yaw / 16.73 do roll).
Ban sua tang do cung actuator tu 100 len 10000 N m/rad:

| | waist_yaw | waist_roll |
|---|---:|---:|
| v21 khong giu | 22.38 do | 16.73 do |
| v24 chi ghi target | 22.38 do | 16.73 do |
| v25 giu cung | **0.26 do** | **0.24 do** |

Nhung sai so bam **gan nhu khong doi**: p95 trai 93.2 -> 89.5 mm, phai
95.3 -> 96.0 mm. Uoc luong hinh hoc 24 mm truoc do dung, nhung sai so thuc bi
chi phoi boi workspace projection (66% target bi chieu, p95 120-134 mm). Gia tri
cua co nay la **loai mot bien gay nhieu** de offline va PhysX so sanh duoc,
khong phai cai thien tracking.

Da chung minh hong **khong the** gay ra lech chi so khuyu: pole tinh trong khung
waist tu khop tay va straightness la goc, nen ca hai bat bien voi phep quay ran
cua hong (thu so: waist 0 do va 11/8.8 do cho ra `32.371478` do y het).

Doc mem da ap cho **ca solver lan plotter**; ban dau chi sua solver nen so sanh
van lech. Quy dao SHA trung khit v22, xac nhan chi doi phep do. Tong trong so hai
ben nay tuong duong (offline 733/705, PhysX 717/676) thay vi 643 vs 794.

Ca hai ban `forward_or_up_fraction` (doc mem) va `..._hard_cutoff` deu duoc ghi
trong metrics de so sanh nguoc voi evidence cu.

## Đồng nhất định nghĩa metric offline — 2026-08-23 (v22)

`REPORT_20260822.md` mục 4 nêu rằng `metrics.json` của v20 còn fail vài elbow
fraction theo ngưỡng dấu cũ, trong khi figure v21 dùng tolerance 5 mm. Việc
chạy lại đã kiểm chứng và khép lại nghi vấn đó.

`t007_mirror_equivariant_natural_continuation_20260823_v22` chạy **đúng như
v20** (resume từ v19, cùng source segment), chỉ đổi một biến: dùng config
chính thức hiện tại, tức thêm `elbow_direction_violation_tolerance_m: 0.005`.
Đó là khác biệt thực chất duy nhất giữa hai config; phần còn lại chỉ là thứ tự
khoá.

**Quỹ đạo không đổi.** `offline_joint_trajectory.npz` của v22 có SHA-256 trùng
khít v20 (`842b7ed390d7985c…`), và cả 27 mảng lệch tối đa `0.000e+00`. Solver
tái lập được với `random_seed: 7`.

Vì quỹ đạo y hệt, mọi thay đổi số liệu dưới đây thuần tuý là **định nghĩa đo**,
không phải thay đổi tư thế:

| Phân số elbow (mẫu số = tập direction_defined) | v20 ngưỡng cũ | v22 tol 5 mm | v21 PhysX tol 5 mm |
|---|---:|---:|---:|
| phải inward | 32.75% | 0.00% | 0.00% |
| phải forward | 29.22% | 1.64% | 1.58% |
| phải upward | 48.49% | 2.39% | 7.72% |
| trái inward | 0.47% | 0.16% | 0.00% |
| trái forward | 3.27% | 2.34% | 0.94% |
| trái upward | 6.07% | 3.27% | 3.29% |

Hai acceptance check từng fail nay đạt, và **chỉ hai check đó** đổi:
`right_elbow_branch` và `right_elbow_not_forward_or_up` từ `False` sang `True`.
Kết luận: bất thường khuỷu phải trong v20 là **tạo tác của ngưỡng đo**, không
phải lỗi tư thế. Ngưỡng dấu cũ đếm cả pole vector nằm trong phạm vi 5 mm quanh
biên là vi phạm.

**Còn tồn tại hai điểm chưa khép.**

1. `scientific_outcome` vẫn là `fail`, nhưng nay chỉ do đúng một nguyên nhân:
   `left_wrist_max` 397.6 mm và `right_wrist_max` 319.9 mm so với ngưỡng
   250 mm. p95 đạt (65.9 mm cả hai bên, ngưỡng 150 mm). Đây chính là peak
   transient ở mục 1 của báo cáo, và là đối tượng của
   time-parameterization toàn quỹ đạo.

2. Sau khi thống nhất tolerance, offline và PhysX khớp nhau ở hầu hết chỉ số,
   nhưng **tay phải vẫn lệch**: upward 2.39% (offline) so với 7.72% (PhysX),
   và `direction_defined_fraction` 56.59% so với 35.92%. Bên trái thì khớp
   (45.76% so với 45.45%). Phần lệch còn lại này là **vật lý**, không phải
   định nghĩa: PhysX đo tư thế sau khi chạy physics, offline đo nghiệm IK.
   Bất đối xứng trái/phải ở mức lệch đó chưa được giải thích và cần điều tra
   riêng trước khi port sang live differential.

Nhắc lại giới hạn: v22 là offline, fixed-base, không Quest live, không DDS,
không phần cứng.
## Latest declared-gesture closeout — 2026-08-22

For the concise Vietnamese closeout covering results, limitations and ordered
next work, see [REPORT_20260822.md](REPORT_20260822.md).

### Decision

The operator-declared sequence from
`runs/t007_whole_upper_body_20260822T112246Z` is now accepted for
**simulation pose sequence and p95 tracking**, but it still fails the strict
maximum-transient-error gate. The evaluated source window is 46.825 s:
initial posture → both arms overhead → both arms lateral → both arms relaxed
along the torso. No Quest live connection, DDS or robot hardware was opened in
the accepted replay.

The final Isaac evidence is
`runs/t007_mirror_equivariant_natural_isaac_20260822_v21`. Its 46.9 s
dual-view video shows the complete ordered gesture. Frame-by-frame inspection
of the 21.2–22.6 s fast overhead-to-lateral transition found continuous V-to-T
motion and no elbow flip. The pose sequence and transition contact sheets are
under `figures_evidence/`.

| Quantity | Left | Right |
|---|---:|---:|
| Projected source target → PhysX wrist, mean | 40.4 mm | 50.3 mm |
| Projected source target → PhysX wrist, p95 | 93.2 mm | 95.3 mm |
| Projected source target → PhysX wrist, max | 489.5 mm | 379.7 mm |
| Command FK → PhysX FK, p95 | 70.0 mm | 68.8 mm |
| Offline wide-phase straightness p05 | 166.0° | 163.0° |
| PhysX bent-arm inward-pole fraction | 0% | 0% |
| PhysX bent-arm forward-pole fraction | 0.94% | 1.58% |
| PhysX bent-arm upward-pole fraction | 3.29% | 7.72% |

The large maxima are brief transition events rather than held-pose failures.
Total time above 250 mm is 0.60 s left and 0.40 s right; the longest continuous
events are 0.30 s and 0.23 s, both around 21.7–22.5 s. This is why the video and
p95 gate pass while the configured 250 mm maximum gate does not. The remaining
control problem is non-causal/vector trajectory time-parameterization for fast
branch transitions, not transport latency or held-pose IK.

Replay operated at 30.000 Hz from the median update period with no hold or
invalid command. Offline trajectory lookup cost was 0.222 ms mean and 0.322 ms
p95. Fresh-or-held replay command age was 17.63 ms mean and 31.74 ms p95. These
are local replay scheduling numbers, not Wi-Fi/network latency measurements.
The fixed root displacement and root velocity were exactly zero.

### Mechanism and matched evidence

The decisive defect was a second, project-local retargeting step. The recorded
wrist matrices were already processed by the vendored `xr_teleoperate`
`TeleVuerWrapper`: Unitree wrist axes and head-yaw-relative
`neutral_waist_yaw_link` positions. Anchoring these poses again to robot
`q=0` distorted shoulder-to-hand radius and forced crouched elbows. The final
pipeline instead uses the absolute vendor pose and projects it onto the
URDF-derived 0.57275 m reach sphere before IK. In this gesture window 66.2% of
left targets and 67.6% of right targets required projection.

Matched failed runs remain evidence:

| Run | Observation | Decision |
|---|---|---|
| `t007_natural_branch_segment_latest_20260822_v14` | relative-to-`q=0`; left wide-phase p05 134.0° | reject: double retargeting bends the elbow |
| `t007_absolute_projected_upstream_continuation_20260822_v15` | upstream full-pose weights; wide p05 176.1°/171.4°, but wrist p95 386.6/383.7 mm | reject: orientation selected a spatially wrong branch |
| `t007_position_manifold_global_reprojection_20260822_v17` | position-manifold global sweep; wrist p95 48.2/52.0 mm | retain as mechanism evidence; pole tolerance/transition not closed |
| `t007_mirror_equivariant_natural_continuation_20260822_v20` | independent right-only canonical mirror solve; offline p95 65.9/65.9 mm and wide p05 166.0°/163.0° | selected for Isaac |
| `t007_mirror_equivariant_natural_isaac_20260822_v21` | complete dual-view replay and PhysX metrics above | pose/p95 pass; transient-max fail |

The upstream `R1_A5_ArmIK` reference was rechecked throughout. Shared elements
are the waist-rooted pose contract, 0.20 m virtual endpoint, URDF bounds,
warm-start/continuity structure and the reference cost
`50 position + 0.5 rotation + 0.02 q² + 0.1 (q-q_last)²`. Upstream has no
explicit elbow-pole or bilateral-symmetry constraint and applies FIR weights
`[0.4, 0.3, 0.2, 0.1]`. The final offline method deliberately differs: wrist
position is the manifold task, orientation weight is zero, a declared natural
elbow sector selects posture, and a trace-level neighbor reprojection reopens
local branches. The right arm is reflected into a left-canonical numerical
problem and mapped back with URDF-verified signs `[1,-1,-1,1,-1]`; it uses only
right-arm targets/history and never copies the left trajectory.

This comparison is now executable rather than only documentary. Every new
offline continuation run audits the vendored `R1_A5_ArmIK` contract before
creating evidence and records the upstream source SHA-256. The currently
audited file hash is
`f395b3ef868ed3c854654b08b33f74ee73f57dbb7c0e8f20440a7f788115a085`.
The audit fails closed if the direct-pose input, virtual endpoint, objective,
IPOPT iteration limit or FIR contract changes; intentional project deviations
remain named explicitly in the resolved run configuration.

### Remaining work

- Replace component-wise causal limiting at fast transitions with a
  vector/non-causal time-parameterization that reduces the 21–22 s peak while
  preserving the position manifold.
- Port the verified branch policy into the live differential controller and
  collect a new Quest run; the offline sweep itself is not real-time.
- Measure actual bridge/network latency in a live run. v21 only measures local
  replay scheduling.
- Re-run simulation after the live port before hardware. This closeout does not
  authorize R1 actuation.
- Ethernet `10.42.0.1/24` and Wi-Fi `192.168.1.106` were not changed.

## Wide-elbow post-review and controlled ablations — 2026-08-22

The operator's review supersedes the earlier visual acceptance below: the
recorded wide gesture is **not accepted**.  At maximum bilateral hand
separation in the differential replay, wrist position error was only about
10.5/6.3 mm, but shoulder–elbow–endpoint straightness was only 133°/125°
(left/right).  The observation therefore isolates elbow/shoulder branch
selection rather than transport, filtering, or endpoint IK accuracy.

Inspection found a real implementation defect: `posture_gain_s` was appended
to the unconstrained velocity after the bounded QP right-hand side had already
been formed.  Coordinate-descent convergence erased that initial-guess-only
term.  The QP now includes the posture preference, with a regression test.
This correction is retained, but it does not solve the missing-observation
problem by itself.

Three morphology-aware ablations were replayed from the same 539-packet source
(`runs/t007_whole_upper_body_20260820T142241Z/raw_commands.jsonl`), all at
30 Hz control and 200 Hz physics:

| Run | Wide/reach elbow result | Wrist result | Decision |
|---|---|---|---|
| `t007_isaac_wide_elbow_headless_20260822T162000Z` | wide frame 155°/152° | wide error 51/58 mm; right p95 161 mm | reject: direct elbow-joint prior selected a poor shoulder branch |
| `t007_isaac_wide_elbow_w1_headless_20260822T170000Z` | wide frame 140°/137° | wide error 32/37 mm; right p95 127 mm | reject: weaker joint prior retained both defects |
| `t007_isaac_wide_elbow_pole_w8_headless_20260822T171000Z` | farthest reach 170°/151°, but widest frame left/right 132°/154° | mean 38/28 mm; p95 109/65 mm | reject: geometric pole improved reach but not the complete gesture |
| `t007_isaac_wide_elbow_pole_early_headless_20260822T172000Z` | left farthest reach regressed to 133° | left p95 505 mm | reject: early activation trapped the left shoulder at a limit |

The pole ablation adds a soft target for the robot elbow on the
shoulder-to-wrist ray and records its activation/error. It is disabled in the
editable profile because none of the tested weights passed the combined wrist
and elbow gate. The best pole run retains complete diagnostic figures under
`analysis/tracking_figures/`; it is evidence of a failed hypothesis, not a
candidate for hardware or final video rendering.

The remaining blocker is temporal branch selection and morphology-aware
retargeting. Full-pose IK can have zero, one, or multiple discrete solutions;
position-only IK usually has a posture manifold, possibly split by limits into
multiple components. The solver must therefore select a posture branch and
keep it continuous over time, not select the smallest-residual frame
independently or activate a new local objective midway through the gesture.
The Quest recording contains no human elbow, so it cannot define the human
branch uniquely, but that does **not** prevent choosing a declared continuous
robot posture policy. The next accepted method is a trajectory-level
continuation solve with wrist position primary, `q[k]-q[k-1]` and posture as
branch selectors, and wrist orientation tertiary. Multi-start is allowed only
to initialize the path; branch changes must be explicit homotopies. Until that
path passes both wrist and elbow gates, no new run should be marked pass and
the local pole heuristic remains disabled.

## Exact Quest-data Isaac replay acceptance — 2026-08-21

### Outcome

The requested recorded-data gate **passes for simulation replay**.  The final
evidence package is
`runs/t007_isaac_replay_final_video_scale075_20260821T080000Z`.  It replays all
539 packets (464 enabled targets) from
`runs/t007_whole_upper_body_20260820T142241Z/raw_commands.jsonl` at the original
intervals (SHA-256
`33570ee1496f5661560c74fc273ce66f3e7052a1418ba7b5cafa1a1a7d2e5678`).  No
Quest, DDS or robot hardware was contacted.

The 18.1 s H.264 video is 640×360 at 10 fps.  Visual inspection of the complete
video and the maximum-reach frames shows continuous shoulder recruitment and
no former inward-folded/crouched elbow branch.  The shoulder–elbow–virtual-EE
angle reaches 149.5° left and 150.9° right at each arm's farthest commanded
reach (180° is geometrically straight).  This is a visually coherent robot
pose, but it is not evidence that the robot elbow matches the human elbow:
the recorded Quest stream contains wrists and head only.

| Quantity | Left | Right |
|---|---:|---:|
| Requested-to-observed wrist error, mean | 20.5 mm | 19.4 mm |
| Requested-to-observed wrist error, median | 16.6 mm | 16.2 mm |
| Requested-to-observed wrist error, p95 | 59.6 mm | 53.5 mm |
| Requested-to-observed wrist error, max | 119.5 mm | 94.8 mm |
| Straightness at maximum target reach | 149.5° | 150.9° |

The source rate is 29.917 Hz over 17.983 s (source-period p95 33.45 ms).
Isaac accepted the enabled stream at 30.000 Hz. Controller compute time is
10.07 ms mean, 11.30 ms p95 and 15.51 ms maximum, below the 33.33 ms control
budget. Fresh-command age is 18.58 ms mean and 33.45 ms maximum. The fixed
root displacement and root linear velocity are exactly zero. Constraint-demand
fractions are 4.08% acceleration, 1.42% velocity, 0.99% reference lead and
0.09% joint limit; no workspace-sphere projection occurred.

### Corrected mechanism and controlled comparison

The complex-motion defect was not primarily transport, filter latency or raw
IK compute time.  The old controller solved unconstrained DLS and then clipped
each joint independently.  In the fast vertical segment near 12.3 s it asked
the left elbow for approximately −13.6 rad/s, clipped it to −3 rad/s, and left
the shoulder contribution unchanged.  That changed the direction of
`J qdot`, drove shoulder pitch into the wrong branch and produced a 0.55 m
transient error.

The corrected controller solves the velocity box constraint inside the DLS
quadratic problem. When one joint reaches a bound, cyclic coordinate descent
re-optimizes the remaining joints. Position/head remain the primary task;
wrist orientation and posture remain secondary. Wrist-position and head
velocity feedforward are enabled, while wrist-orientation feedforward is
excluded because it caused all ten arm joints to saturate on this trace.

| Replay variant | Key evidence | Decision |
|---|---|---|
| scale 0.85, old clip | wrist p95 556/625 mm; velocity demand 20.9% | reject |
| scale 0.65, old clip | p95 203/131 mm; far-reach angle 135°/133° | reject |
| scale 0.75, full pose feedforward, old clip | p95 515/509 mm; velocity demand 20.1% | reject |
| scale 0.75, position feedforward, old clip | left p95 322 mm; wrong shoulder branch | reject |
| scale 0.75, bounded DLS, headless | p95 53.9/51.6 mm at 30.000 Hz | retain as timing A/B |
| scale 0.80, bounded DLS | left p95 476 mm; joint-limit demand 8.06% | reject |
| scale 0.75, bounded DLS, video | p95 59.6/53.5 mm at 30.000 Hz | **accept replay** |

The editable profile is now scale 0.75, 3 rad/s, 30 rad/s², bounded DLS and
`wrist_position_head` feedforward.  The 3 rad/s controller bound remains far
below the selected URDF joint velocity limits (minimum 18.8 rad/s), but it is a
simulation control choice, not a hardware authorization.

For the planned live verification, `make teleop-arms` now selects the same
validated runtime envelope by default: arms/head only, physics 200 Hz, control
30 Hz, one video camera at 10 fps, and host IP `192.168.1.106`. The matching
certificate and key are present. Override these defaults only if the resulting
run is intentionally treated as a different protocol.

### Evidence and rerun path

- `simulator_view.mp4` — final pose/trajectory video;
- `metrics.json` — wrist, head, joint, timing, signal-age, constraint and fixed
  root metrics;
- `analysis/tracking_figures/endpoint_target_vs_achieved.png` — wrist x/y/z
  target, bounded command, PhysX observation and unsmoothed error;
- `analysis/tracking_figures/endpoint_velocity_tracking.png` — endpoint
  velocities;
- `analysis/tracking_figures/joint_reference_vs_physx.png` — every controlled
  arm/head joint;
- `analysis/tracking_figures/controller_and_signal_timing.png` — compute time,
  update period and fresh-command age;
- `analysis/tracking_figures/fixed_base_root_and_joint_trace.png` and
  `per_link_com_height.png` — fixed-base and per-link dynamics;
- `resolved_config.json`, `experiment_runner_command.txt`, raw/target records
  and hashes — exact provenance and reproduction path.

### Remaining limitations

1. The replay gate does not validate a fresh live session.  The planned
   operator rerun remains necessary because calibration pose, tracking loss and
   real input jitter may differ tonight.
2. Human shoulder/elbow pose was not recorded.  The video validates a coherent
   robot IK branch and wrist trajectory, not human-to-robot upper-arm pose
   correspondence. Meta elbow tracking or an explicit elbow-out pole task is
   still required for that stronger claim.
3. The conservative outer reach sphere cannot represent every joint-limited
   hole in the true workspace. Scale 0.80 exposed this on the left arm. A
   joint-feasible workspace projector remains useful for trajectories beyond
   this trace.
4. A dual-view 15 fps render reduced enabled control updates to 24.62 Hz. The
   accepted run uses one camera at 10 fps and keeps 30 Hz; dual-view rendering
   is diagnostic only until it runs asynchronously.
5. Isaac startup and shutdown can stall independently of the measured control
   loop; the runner force-exits after artifacts are finalized. This does not
   change the recorded 18 s controller timing but remains an operational issue.
6. Hardware remains blocked by the normal safety gate: hardware-specific
   limits, suspended fixture, E-stop operator and a new live simulation run are
   required.

The requested network state was checked after the replay: Ethernet `enp78s0`
is `10.42.0.1/24`; Wi-Fi `wlp77s0` is `192.168.1.106/24`; the only default
route is via `192.168.1.1` on Wi-Fi.

Teleop discovery passes 214 of 216 tests after adding bounded-solver and arm
straightness regressions. The two failures are the same pre-existing missing
historical T001 catalog artifacts documented below; neither exercises T007.

## Differential live-control closeout — 2026-08-20

### Status

The live differential-IK work is **closed as an informative failure, not an
accepted tracking result**.  Solver latency and transport are no longer the
dominant uncertainty.  The remaining failure is retargeting and elbow-branch
selection: the Quest stream used here contains head and wrist poses but no
measured shoulder, upper-arm or elbow pose.  A wrist target alone does not
uniquely determine the robot elbow swivel or shoulder configuration.

The retained diagnostic evidence package is
`runs/t007_whole_upper_body_20260820T142241Z`.  It contains the 68.2 s H.264
single-view video, 468 enabled samples, raw commands, resolved configuration,
joint/physics traces, timing and complete figures under
`analysis/tracking_figures/`.  This run used relative-session scale 0.5 and is
intentionally retained because it exposes the bent-elbow failure clearly.

### What was implemented

- One-step differential DLS replaced iterative pose IK in the live controller.
- The first enabled sample latches the measured robot pose; later wrist and
  head motion is relative to that session origin.
- Wrist targets are projected into the URDF-derived conservative shoulder
  reach sphere before solving.
- Position/head are the primary task and wrist orientation is a lower-priority
  null-space task.  A position-null-space posture prior discourages the
  orientation task from selecting an extreme elbow branch.
- Velocity, acceleration, next-step joint-limit and reference-lead constraints
  remain active; the pelvis and legs remain fixed and base commands remain
  prohibited.
- RTX rendering is scheduled at the 15 Hz evidence-video rate rather than at
  every control tick.  This restored the requested 30 Hz control rate.
- Diagnostics now include source/calibrated/projected wrist targets, joint
  reference and PhysX state, controller compute time, command age, projection,
  singular value and per-joint constraint activation.

JAX was not installed into `unitree_sim_env`: the environment contains Isaac
Sim 5.1.0 but no compatible JAX build.  The NumPy central-difference backend is
already below the 33.3 ms budget, so modifying the vendor environment would not
address the observed kinematic failure.

### Controlled evidence

| Run / check | Observation | Interpretation |
|---|---|---|
| `t007_whole_upper_body_20260820T134059Z` | Absolute 1:1 mapping; 74.37% of enabled samples projected; wrist error mean 0.260/0.401 m; velocity constraints active 38.84% | Absolute frame/amplitude mapping was invalid; compute was not the cause. |
| `/tmp/r1_diff_relative_smoke_20260820` | Relative scale 0.4; 30.00 Hz; compute p95 10.63 ms; no projection; wrist p95 about 6.1 mm | Relative calibration removed the startup jump and solved the gentle synthetic trajectory. |
| `t007_whole_upper_body_20260820T140045Z` | Relative scale 0.4, weighted DLS; no projection; mean wrist error 37.6/38.4 mm; effective enabled update 20.08 Hz | Mapping improved greatly, but per-tick rendering and pose-feedback lag remained. |
| `t007_whole_upper_body_20260820T141423Z` | Strict position→orientation priority; shoulder/wrist motion increased, but mean wrist error rose to 50.5/48.6 mm and constraints increased | Giving wrist orientation too much null-space authority selected visually poor elbow branches and consumed constrained joint motion. |
| `/tmp/r1_diff_elbow_branch_smoke_20260820` | Orientation weight 0.1, posture gain 1.0, scale 0.5; 30.00 Hz; compute p95 9.39 ms; wrist p95 about 8.0 mm; no saturation/projection | The reduced-orientation elbow prior is numerically healthy on the declared synthetic sweep. |
| `t007_whole_upper_body_20260820T142241Z` | 30.00 Hz; compute p95 9.29 ms; fresh-command age mean 19.3 ms; wrist error mean 31.7/28.1 mm and p95 78.8/70.6 mm; no projection; root motion exactly zero | Timing path is healthy. Video still shows bent elbows during the operator's full lateral extension, so the run fails the qualitative motion requirement. |

At maximum lateral extension in the last live run, the scale-0.5 targets were
only 0.411 m (left) and 0.398 m (right) from the corresponding shoulder, versus
the 0.573 m conservative URDF reach bound.  The controller therefore received
a bent-arm target even though the operator had fully extended their arms.  For
the same recorded source samples, relative scale 0.85 maps the targets to
approximately 0.564–0.570 m.  At this 2026-08-20 closeout the editable profile
temporarily used 0.85 as an unvalidated candidate; the 2026-08-21 replay above
subsequently rejected it and selected 0.75 with bounded DLS.

The main artifacts from the retained last live run are:

- `runs/t007_whole_upper_body_20260820T142241Z/simulator_view.mp4` — temporal
  evidence of the remaining bent-elbow behavior;
- `analysis/tracking_figures/endpoint_target_vs_achieved.png` — calibrated
  target, differential reference and observed FK position;
- `analysis/tracking_figures/endpoint_velocity_tracking.png` — endpoint
  velocity;
- `analysis/tracking_figures/joint_reference_vs_physx.png` — all 12 controlled
  joint references and PhysX positions;
- `analysis/tracking_figures/controller_and_signal_timing.png` — solver time,
  accepted update period and fresh-command age;
- `analysis/tracking_figures/fixed_base_root_and_joint_trace.png` and
  `per_link_com_height.png` — fixed-root, joint and per-link COM diagnostics.

The nine focused differential-tracking, calibration and workspace-projection
tests pass.  The broader `test_r1_*.py` discovery passes 211 of 213 tests.  Its
two failures predate this controller and both concern missing historical T001
catalog artifacts (`t001_a_20260802_quest01` and T001-B bridge-log targets);
they are recorded rather than repaired inside the T007 closeout.

### Historical remaining work at the 2026-08-20 closeout

Items below preserve the state before the exact-data replay. The 2026-08-21
section above records which items were completed or superseded.

1. **Live-validate scale 0.85.** Start with controllers in a declared neutral
   pose, latch once, then repeat full lateral extension, forward reach, overhead
   reach and cross-body motion.  Keep the run failed unless the video and
   quantitative straightness metric both pass.
2. **Add an elbow measurement or an explicit pole-vector task.** Preferred:
   ingest Meta upper-body/elbow tracking if its frame/rate/validity can be
   verified.  Fallback: derive a side-specific elbow-out pole vector and solve
   it between wrist position and wrist orientation priorities.  Wrist-only IK
   must not be described as shoulder tracking.
3. **Add a geometric arm-straightness metric.** Compute the
   shoulder–elbow–wrist angle from FK and evaluate it at source lateral-reach
   extrema.  Joint angle alone is insufficient because the URDF contains
   non-zero shoulder frame rotations and link offsets.
4. **Make constraints hierarchical.** The current unconstrained strict-priority
   velocity is box-clipped afterward; under fast motion a lower-priority
   orientation command can still consume velocity/acceleration authority.
   Allocate remaining bounds to secondary tasks or use the documented QP.
5. **Repeat the full evidence run.** Required outputs remain video, raw and
   resolved configs, source/projected/observed endpoint traces, endpoint
   velocity, every joint, controller timing, transport age and fixed-base
   dynamics.  The candidate scale-0.85 config must not replace the failed live
   run in a claim until this rerun exists.
6. **Hardware remains blocked.** Simulation evidence does not authorize robot
   actuation.  A verified elbow policy, hardware-specific limits, suspended
   fixture, E-stop operator and the repository safety procedure are still
   required.

Network configuration was left as requested: Ethernet `10.42.0.1/24` with no
default route and Wi-Fi `192.168.1.106/24` with the Wi-Fi default route.

## MuJoCo trajectory-tracking replay — 2026-08-20

`t007_mujoco_replay_20260820T122648Z` replays the longest contiguous enabled
segment from `t007_whole_upper_body_20260819T030356Z`: 2,559 Quest packets over
85.44 s. The input path is calibrated relative to the first sample and scaled
to 0.4 so it remains inside the R1 arm's conservative reach bound. This changes
workspace amplitude and is not comparable with the earlier absolute 1:1 runs.

The new controller follows the theory note's velocity-level structure at
100 Hz: filtered Cartesian velocity feedforward plus pose feedback, weighted
damped least squares, acceleration/velocity constraints, joint-limit-aware
integration and actuator position tracking. MuJoCo runs at 500 Hz. The primary
`arm_pose_head_velocity_feedforward` hybrid produced:

| Metric | Left | Right |
|---|---:|---:|
| Wrist position RMSE | 11.95 mm | 11.89 mm |
| Wrist velocity RMSE | 0.0854 m/s | 0.0896 m/s |
| Head pitch/yaw RMSE | 0.00562 rad | 0.01185 rad |

Controller computation was 4.98 ms mean and 5.23 ms p95, below the 10 ms
control period. The source stream was 29.938 Hz with no sequence gap in the
selected segment; the source run's same-host fresh-command age was 17.06 ms
mean and 33.02 ms p95. Joint-limit constraints were active for 0.413% of
joint-ticks, no commanded joint crossed its limit, and no simulated actuator
hit its effort limit. All declared simulation criteria passed.

The ablation in `t007_mujoco_replay_20260820T122119Z` matters. Full arm
velocity feedforward improved the left wrist and head but worsened right-wrist
position and both wrist-velocity RMSE values. Because the `arms_head` model is
kinematically separated, the selected hybrid keeps pose feedback on both arms
and applies filtered angular-velocity feedforward only to the head. Full linear
latency prediction was worse and is not selected.
Earlier low-damping trials oscillated near the right-arm singular region;
raising damping to 0.1 removed that oscillation. A 0.35 scale caused a branch
transition with 0.410 m peak error and was rejected rather than hidden.

Reproduce with:

```bash
MPLCONFIGDIR=/tmp/mpl conda run --no-capture-output -n r1_env python \
  scripts/teleop/run_r1_t007_mujoco_replay.py
```

This is fixed-base MuJoCo CPU evidence, not IsaacLab equivalence or hardware
readiness. The canonical MuJoCo R1 asset has fixed head visuals but no head
joints, so the runner reconstructs the two URDF head joints in the run-local
`resolved_mujoco_model.xml`; the canonical asset remains unchanged.

## Folded-posture local minimum — 2026-08-18

`t007_whole_upper_body_20260818T114338Z` ran with the corrected head convention
and `body_mode=arms_head`: head tracking error fell to 0.002 rad mean, no target
was refused, and the rate rose to 9.98 Hz. It still showed a wrong elbow
whenever the operator drew the hands in towards the body.

That is a solver failure, not a reach limit. Binned by distance from the
shoulder, targets 0.20–0.30 m away — well inside the arm, which can fold to
0.152 m and extend to 0.510 m — carried a **0.203 m mean position residual**
while `shoulder_yaw` and `elbow` barely moved (spans of 3° and 5°). Further out
the residual was smaller.

The cause is the continuation seed. Re-solving the worst such sample:

| Seed | Position residual |
|---|---|
| previous solution (as live) | 0.2068 m |
| declared nominal | 0.0297 m |
| best of 40 random seeds | 0.0129 m |

A good solution exists; the extended previous posture simply cannot cross into
the folded basin. Raising `max_iterations` to 400, enlarging
`max_joint_step_rad`, lowering `damping`, and weakening the wrist-orientation
weight by 100× all returned **exactly 0.2068 m at iteration 31** — none of them
changes the basin, only the seed does.

The sink now re-solves once from the declared nominal when a solve stalls above
`seed_restart_residual_m` **and** both targets lie inside the arms' reach bound.
That bound is derived from the URDF by triangle inequality over the link
translations plus the tool offset (0.5828 m against a 0.5584 m sampled maximum),
so it is conservative by construction rather than a tuned constant. The gate
matters: without it the restart also fires on genuinely out-of-reach targets and
costs 261.7 ms per target, while with it the cost is unchanged.

Replaying the run's 297 targets through the sink, changing only this:

| | close-in mean | close-in max | all mean | cost |
|---|---|---|---|---|
| before | 0.3815 m | 0.5943 m | 0.2788 m | 157.0 ms |
| after | **0.0010 m** | **0.0025 m** | 0.0762 m | 162.5 ms |

Only 10 restarts fired across 297 targets. Their value is disproportionate
because one trapped solve poisons the seed for every frame after it.

The exact failing sample is pinned as a regression in
`tests/teleop/test_r1_upper_body_ik.py` as literals, so it does not depend on
the evidence directory.

This is offline replay of a recorded trace, not a new live run. Achieved rate,
smoothness and post-physics tracking under the restart remain unassessed, and
the run's own `scientific_outcome` stays `unassessed`.

## Head-target convention defect and body modes — 2026-08-18

`t007_whole_upper_body_20260818T072252Z` recorded a body pose that did not
follow the operator while hands and head tracked acceptably, and ran at
**8.57 Hz against a requested 20 Hz**. Both symptoms trace to one defect.

The sink built the head target as `Rz(yaw) @ Ry(pitch)`, but the R1 head chain
applies `head_pitch` before `head_yaw`. The two orders differ whenever both
angles are non-zero, so the head target was **not realizable** by the two head
joints. Consequences, measured from that run's evidence:

- **0 of 334** dispatched targets converged (the earlier run: 687/687).
- Mean solver iterations rose from **2.4 to 34.5** against a cap of 40, so IK
  cost **297 ms** per solving step versus 55.5 ms before, against a 50 ms
  budget. IK consumed 56.9% of wall time; steps without IK held 50.4 ms, i.e.
  the loop was otherwise correctly sized for 20 Hz. This is the slowdown.
- Chasing the unsatisfiable head orientation, the solver recruited
  `waist_yaw` — range ±150°, opposed only by a null-space posture bias 200×
  weaker than the position task — which ratcheted across frames from 17° to a
  **108.4°** twist. This is the body-pose symptom.

The run's operator turned their head far more than the previous one (mean
|yaw| 67.8° versus 11.2°, both angles above 10° on 17.1% of samples), which is
why the defect surfaced then and not earlier. The earlier run is not evidence
that the convention was correct.

The head target is now derived from the head chain's own FK. Offline replay of
the same 334 targets, with no other change, gives |waist_yaw| max **10.1°**
(from 108.4°) and mean head residual **0.0137 rad** (from 0.1199).

Two further findings are recorded rather than tuned around:

- Solution quality is **not monotonic** in the iteration budget. On this trace
  12 and 40 iterations both behave (|waist_yaw| max ≈ 10°), while 20 diverges
  to 136.8°. The iteration cap is therefore not a safe tuning dial, and the
  40-iteration cap is retained.
- The second evidence camera added in the same change costs ~1.9 ms per step
  (48.5 → 50.4 ms) and is not a material contributor to the rate.

A `body_mode` selector now chooses which torso joints the solver owns:
`arms_head` (12 DoF, torso frozen), `waist_yaw` (13, hardware-common) or
`full_upper_body` (14, adds waist roll as a simulation-only deviation).
`arms_head` removes the waist from the task, so it cannot be recruited at all;
`make teleop BODY_MODE=arms_head` selects it without editing the profile.

All prior schema-3 runs remain immutable legacy evidence and are **not**
comparable with runs made after this correction. No live run has yet been made
with the corrected convention: the figures above are offline replay of a
recorded target trace, not new Quest evidence, and tracking, smoothness and
achieved rate remain unassessed on hardware or in a fresh live session.

## Boundary projection, waist-roll option and one-command launch — 2026-08-18

`t007_whole_upper_body_20260818T050419Z` completed but refused 133 of its 820
enabled targets with `upper_body_ik_refused:iteration_budget_exhausted`, most
repeatedly clamped at `left_shoulder_roll_joint`. Because the coupled sink held
all 13 joints on any non-converged solve, each refusal froze the whole upper
body, which is what an operator sees as arms that stop extending and a
trajectory that stops being followed.

Three changes follow. All are protocol changes, so this run and every earlier
schema-3 run remain immutable legacy evidence and are not comparable with runs
made after this entry.

1. **Boundary projection.** The coupled solver now returns the closest
   reachable iterate as `projected_to_reachable_boundary`, matching the
   single-arm solver. Its stagnation test is relative rather than an absolute
   `1e-14` score delta, which never fired for far targets. The editable profile
   sets `allow_projected_position_solution: true`, so a projected target is
   dispatched and recorded as `solver_solution_kind: "projected"` with its
   residual instead of freezing every joint. `singular_system` still holds.
2. **Optional waist roll.** `control_waist_roll: true` adds `waist_roll_joint`
   as a 14th controlled joint for torso lean. It is a declared simulation-only
   deviation — the R1-A5 motor interface marks that slot unused — and defaults
   to `false`. See `docs/teleop/05_project_solvers_and_evidence.md`.
3. **Second evidence camera and one-command launch.** `--dual-view` records a
   mirrored second fixed camera and stores both views side by side in one
   synchronized video. `make teleop` (or
   `scripts/teleop/run_t007_upper_body_pilot.py`) replaces the hand-typed
   two-process pipeline below and allocates the run id, stop file and evidence
   directory the same way the T001-B launcher does.

Dispatching those previously-frozen targets exposed a fourth, pre-existing
defect: the second-order rate limiter could carry a command 0.133 rad past
`left_shoulder_roll_joint`'s lower limit when a target reversed direction,
because the acceleration limit cannot cancel stored velocity in one step and
nothing clamped the limiter's output. The coupled sink now passes the model's
joint limits to the limiter, which clamps and zeroes the velocity of clamped
joints. After the fix the same replay dispatches nothing outside any joint
limit. The legacy per-arm sink retains the unclamped limiter; changing it would
alter schema-2 comparability and was left alone.

Offline replay of this run's 820 recorded targets through the revised solver
raises dispatch from 591/820 (72.1%) to 820/820: 491 exact (0.7 mm mean
position residual) and 329 projected (366–429 mm mean). That large residual is
the real finding — the recorded Quest targets reach 0.954 m from the pelvis
against roughly 0.750 m of arm reach, so the operator is reaching outside the
R1 workspace at 1:1 absolute scale and the arm can only extend to its boundary.
Whether to keep 1:1 absolute mapping or introduce a scale is an open protocol
question, not something this change decides.

**Not yet verified:** no live Quest/Isaac run has been made under these
changes. The replay harness measured mean solve time rising from 63.6 ms to
77.3 ms per target with p95 and maximum unchanged, but workstation replay
timing is not comparable with the live `unitree_sim_env` environment. The next
live run must confirm the achieved control rate, the dual-view video, and the
post-physics tracking before any pass claim.

## Schema 3 coupled whole-upper-body pilot — 2026-08-18

The new optional path solves `waist_yaw_joint`, both five-joint arms, and both
head joints in one 13-DoF IK problem. It keeps the root, legs, and
`waist_roll_joint` fixed. This changes the method and joint ownership, so its
runs are not directly comparable with the schema-2 independent-arm evidence
described below. The method is `docs/teleop/05_project_solvers_and_evidence.md`; the editable
pilot configuration is `config/r1_t007_whole_upper_body_live.json`.

Run from the repository root. The launcher allocates the run id, stop file and
evidence directory, and starts both processes:

```bash
make teleop HOST_IP=10.42.0.1
```

`make teleop-dry-run` prints the allocated paths and both underlying commands
without starting anything. The equivalent explicit form is:

```bash
python3 scripts/teleop/run_t007_upper_body_pilot.py --host-ip 10.42.0.1 --duration-s 180
```

The left trigger resets all 13 targets to the declared zero pose; the right
trigger enables fresh input. Stop with `Ctrl-C` and wait for the evidence path
to print. This command has passed CLI/static validation but has not yet produced
a schema-3 Quest/Isaac evidence run; tracking and timing therefore remain
unassessed.

The synthetic smoke
`results/smoke/t007_whole_upper_body_preflight_20260818T000000Z` exercised the
initial schema-3 runner at 20.06 Hz with zero root displacement/velocity and no
base dispatch, but rejected 8 of 26 enabled targets after spending its
iteration budget reducing a secondary null-space posture cost. That exposed a
method error: nominal posture was being treated as a convergence gate even
after wrist/head task tolerances passed. The solver now terminates on the
declared physical task tolerances and keeps posture only as a secondary cost.
Offline replay of the same 26 targets then accepted all 26 in at most 11
iterations (14.0 ms/target mean on this host). The original smoke is retained
as pre-revision diagnostic output and is not evidence for the corrected method;
the corrected simulation smoke is reported next.

The corrected synthetic smoke at
`results/smoke/t007_whole_upper_body_preflight_20260818T044251Z` accepted all
100 enabled targets, sustained 20.06 Hz, dispatched no base command, and kept
root displacement and velocity at exactly zero. Its pure IK position residual
was below 1.9 mm on both arms. This distinguishes the remaining dynamic error
from the geometric solve: requested-to-observed endpoint error was 69.8 mm
left and 83.9 mm right on average, while rate-limited-command-to-observed error
was 57.6/53.4 mm. The largest persistent component in the diagnostic figure is
vertical sag of the simulated articulation under its current actuator model,
not an IK reach failure. The generated unsmoothed figure is
`figures/endpoint_target_vs_achieved.png` inside that smoke directory. This is
still synthetic smoke input, not live Quest evidence or hardware validation.

T007 is now executable as a simulation-only bilateral arm/head pilot. It uses
the same Quest bridge stream as T001-B but, unlike T001, sends the two wrists
through the reviewed 5-DOF IK and drives both arms plus head yaw/pitch. Base
velocity remains hard-disabled. The first deadman-held sample is the session
start marker. Arm positions and wrist roll use the vendor's absolute waist-frame
pose at scale 1:1; the first sample does not redefine an arm neutral.

This controls every direction that the current model can represent: reachable
3-D wrist translation, wrist axial roll, and head yaw/pitch. The current live
profile sends every target to IK first (there is no rectangular workspace
prefilter). Exact reachable targets are solved normally; targets outside the
bounded joint workspace are projected to the closest iterate at the reachable
boundary and marked `projected` with their residual. It does **not** control fingers, wrist pitch/yaw,
torso/waist, balance, or locomotion. A target outside the five-DOF arm's true
reachable set is never approximated by base motion. A malformed solve, stale
input, deadman release or sequence fault freezes the last complete arm/head
target.

T007 now creates a true fixed-base articulation (`fix_root_link=True`), rather
than repeatedly overwriting a free root state. Every arm/head run saves
`dynamics_trace.npz`: root position/velocity, all joint positions/velocities,
and every link's world-frame COM position. Generate diagnostic figures after a
run without altering raw evidence:

```bash
MPLCONFIGDIR=/tmp/mpl conda run --no-capture-output -n r1_env python \
  scripts/teleop/plot_r1_t007_dynamics.py \
  experiments/r1_teleop/quest3_sim_v1/T007/runs/<run-id>
```

For schema-2 runs this also produces `endpoint_target_vs_achieved.png`: raw
Quest virtual-EE targets, FK of the rate-limited joint command, and FK
reconstructed from the post-physics arm joints, plus unsmoothed error
components. Runs made after the 2026-08-17 diagnostics revision additionally
record the pre-limiter IK joint target; older schema-2 runs retain their scalar
IK residual. Black samples are targets the IK explicitly projected to the
reachable boundary. This separates physical workspace mismatch, rate-limit lag
and simulator tracking lag; schema-1 runs are deliberately not reinterpreted
with the new endpoint model.

Cò trái returns both arms and head to joint zero (not the last held pose),
clears the active session, and holds there. Release it, then hold cò phải to
apply the current absolute Quest pose.

The simulation also starts at that same declared joint-zero pose. It no longer
spawns in Isaac's bent-arm default and spends the first seconds rate-limiting an
unrelated transition before following the first Quest target.

Run from repository root with the headset connected. Cò trái is an
edge-triggered reset: it returns the robot to the **declared neutral arm/head
pose**, clears the current session, and requires a fresh current pose on the
next cò phải press. It never reuses the stale pre-disconnect Quest pose.

```bash
run_id="t007_live_arm_head_$(date -u +%Y%m%dT%H%M%SZ)"
conda run --no-capture-output -n tv python scripts/teleop/quest_bridge.py \
  --host-ip 10.42.0.1 --duration-s 180 \
  --trigger-value-threshold 5.0 \
  --connection-log "experiments/r1_teleop/quest3_sim_v1/T007/runs/${run_id}.bridge.jsonl" \
| conda run --no-capture-output -n unitree_sim_env python scripts/teleop/run_r1_quest3_live.py \
  --config experiments/r1_teleop/quest3_sim_v1/T001/config/r1_quest3_sim_v1.json \
  --arm-head-config experiments/r1_teleop/quest3_sim_v1/T007/config/r1_t007_arm_head_live.json \
  --output-dir "experiments/r1_teleop/quest3_sim_v1/T007/runs/${run_id}" \
  --duration-s 180 --physics-hz 100 --control-hz 20 --disable-self-collisions \
  --idle-stop-s 0
```

Use `Ctrl-C` in the pipeline or create the declared `--stop-file` to end it
gracefully; evidence is written before Isaac shutdown. This is not yet a T007
pass: inspect the recorded arm/head targets, observed joint states, hold events
and video, then compare the exact raw trace offline before claiming equivalence.
Collision remains inconclusive under the asset workaround.

The controller gate accepts either TeleVuer's digital trigger flag or its
validated analog pull value at or below `5.0` on the wrapper's inverted
`10=released, 0=fully pressed` scale. This fallback is necessary because the
2026-08-17 Quest session delivered poses while leaving both digital trigger
flags false. Every effective press/release transition, the raw boolean, and the
analog value are recorded in the bridge JSONL. `--idle-stop-s 0` prevents the
simulator process from terminating during a recoverable WebXR pose gap; the
mapper still enters `command_timeout` hold, and the operator stops the bounded
run with `Ctrl-C` or its stop file.

## Initial live outcome and protocol revision — 2026-08-11

`t007_live_arm_head_20260811T074737Z` completed with no received command and
is retained as an execution/connection observation. The subsequent live run,
`t007_live_arm_head_20260811T080127Z`, received 4,753 normalized commands.
Fresh-command age was 0.53–40.14 ms (mean 16.85 ms), so this run does not show
that the host/Quest transport itself is the primary realtime bottleneck.

It did reveal a protocol limitation: only 578 arm/head dispatches were
accepted, while 1,457 holds came from leaving the selected workspace envelope
and 114 from IK refusal; other holds were deadman and timeout events. This is
consistent with the observed failure to follow large extension motions, and is
not a valid claim of full-workspace tracking.

After that run, the editable T007 config was revised from position scale 1.0 to
0.3 and the command protocol gained an explicit operator lifecycle: a rising
left-trigger edge returns the simulated arms/head to the declared neutral pose
and clears the calibration; the next right-trigger command captures a fresh
relative neutral. The two initial runs use the old no-reset/scale-1.0/free-root
behavior and are retained but are not comparable to a run made under the
revised config. The next live run must be assessed separately from its own
resolved configuration and calibration event record.

## Workspace-prefilter revision — 2026-08-11

`t007_fixed_base_20260811T084211Z` verified the fixed root but still recorded
151 holds from the legacy rectangular prefilter, before IK could test those
targets. The editable live profile now uses `workspace_policy=ik_joint_limit_only`
(`enforce_workspace=false`): it removes that prefilter but retains the IK hard
joint limits, convergence checks, deadman and timeout holds. A solution that
converges exactly at a position-joint limit is now dispatched and explicitly
recorded; it is not treated as an IK failure. The current schema-2 profile
clamps a roll request beyond its wrist limit and records that status. This is a
simulation-only policy, not a hardware policy.
This is a protocol compatibility break. All earlier T007 runs remain immutable
legacy observations; the next run is the first evidence for the new policy.

## Configuration-snapshot correction — 2026-08-11

The runner initially stored only the mutable pathname of the T007 arm/head
profile, while `experiment_config.json` contained the shared T001 bridge
config. From the next run onward, `experiment_config.json` is the immutable T007
profile, `bridge_config.json` snapshots the bridge configuration, and both
hashes are in `metadata.json`. `t007_ik_full_reach_20260811T085504Z` is retained
as an observed functional run but is not a reproducible quantitative baseline
because it predates this correction.

The same correction adds requested target position, solver status, residual,
limit margin and clamped-joint names to every later `ik_refused` event. It makes
the next run suitable for identifying whether a missed extension was genuinely
outside the arm workspace, a roll-limit request, or a solver-basin failure.

## R1-A5 endpoint and absolute-mapping correction — 2026-08-17

Inspection of `t007_fixed_base_20260811T084211Z` and the vendored R1-A5 IK
identified three coupled method errors. The old T007 profile used a 0.3 motion
scale; its declared neutral solved to about -0.716 rad elbow instead of joint
zero; and the local solver controlled the wrist joint origin while the vendor
R1-A5 IK defines its virtual end-effector 0.20 m along wrist local +x. After
adding that tool offset, the first implementation also exposed a Jacobian bug:
the Jacobian still differentiated the wrist origin rather than the corrected
end-effector.

Schema 2 fixes all four points: FK and Jacobian use the same vendor endpoint,
reset writes the declared q=0 pose directly, arm motion is absolute vendor pose at scale 1:1,
and unreachable position targets are explicitly projected to the bounded joint
workspace instead of freezing both arms. This invalidates all earlier T002-T007
workspace/IK results for comparison with new runs; they remain preserved as
legacy evidence.

The first schema-2 Isaac smoke exposed one additional startup artifact: the
articulation still spawned in its bent default joint pose even though reset q=0
was declared. The runner now writes q=0 to both arms/head before the control
loop and synchronizes the IK seeds and rate limiters to it. This change applies
only to runs created after 2026-08-17.

## Fixed-base trace smoke — 2026-08-11

The synthetic, simulation-only preflight at
`results/smoke/t007_fixed_base_preflight_20260811T090000Z` verified the new
trace path and the physical root constraint: root maximum displacement and
linear velocity were both `0.0`, across 40 recorded link COM traces. It also
produced the two diagnostic PNGs from the immutable raw trace. This is a
software smoke, not a Quest evidence run and not evidence that the full arm
workspace is reachable.

## Schema-2 wide-arm smoke — 2026-08-17

The CPU IK regression reaches the mirrored wide targets
`[0.25, ±0.55, 0.10]` m with residual below the configured 2 mm tolerance.
The final simulation-only smoke at
`results/smoke/t007_ik_schema2_preflight_20260817T004500Z` then swept both
virtual endpoints between `[0.40, ±0.20, 0.10]` and that wide target. All 65
enabled targets were dispatched, none was marked projected, base dispatch count
was zero, achieved control rate was 20.21 Hz, and fixed-root displacement and
velocity were both exactly zero in the recorded trace.

At the widest sample, observed FK reached `[0.253, ±0.534, 0.054]` m against
`[0.25, ±0.55, 0.10]` m, a 48.7 mm dynamic error dominated by vertical lag.
Therefore this smoke confirms the corrected IK path and lateral extension, but
does **not** establish accurate dynamic tracking. The unsmoothed source figure
is `figures/endpoint_target_vs_achieved.png` inside that smoke directory. Two
preceding attempts ending in `T000000Z` and `T001500Z` received zero enabled
targets because their synthetic commands expired during Isaac startup; they are
retained as failed timing checks and are not included in the result above.

This is synthetic input without a Quest and belongs under `results/smoke`, not
T007 immutable experiment evidence. A new live run is still required.

## Live schema-2 analysis and rate revision — 2026-08-17

`t007_live_arm_head_20260817T114118Z` is the first complete live run after the
endpoint, startup-pose and trigger corrections. It completed by operator
`SIGINT`, recorded all required artifacts and video, received 3,150 valid
commands, dispatched 1,634 bilateral arm/head targets, and never dispatched
base velocity. The articulation root displacement and velocity were both
exactly zero. Trigger transitions show right deadman and both left-trigger
reset edges were received through the revised digital/analog input path.

This run remains `scientific_outcome=unassessed`: almost half of the enabled
samples were outside exact joint-limited reach (716 left, 735 right). Mean IK
position residual was 0.0242 m left and 0.0322 m right, with maxima 0.1956 m
and 0.2389 m. The larger visible mismatch came after IK: requested endpoint to
rate-limited command was 0.1276/0.1313 m mean and 0.5163/0.4868 m p95; command
to observed articulation was 0.0502/0.0489 m mean and approximately 0.089 m
p95. Therefore loosening the geometric workspace or changing posture bias
would not address the dominant lag.

An offline replay over the exact target trace kept the IK and posture method
fixed and varied only the simulation rate pair. Changing from `1.0 rad/s`,
`2.0 rad/s²` to `1.5 rad/s`, `4.0 rad/s²` reduced expected mean
requested-to-command error to 0.0778/0.0802 m and p95 to 0.3422/0.3154 m.
Higher pairs reduced replay error further but were not selected: the requested
change was modest, and replay does not include any resulting articulation lag.
The editable config now carries the selected pair and its source. This is a
protocol parameter revision, not an observed improvement; a new live run must
verify smoothness and post-physics tracking before replacing the old result.

New runs also record pre-limiter IK joint targets and limiter velocities, so
future figures can separate IK residual, rate-limit lag and articulation lag
without reconstructing hidden solver state. The plotter now draws requested,
rate-limited-command and observed endpoints with all three error components.

## Offline whole-trace branch continuation — 2026-08-22

The first complete continuation artifact,
`t007_offline_continuation_v6_20260822T203000Z`, is retained as contradictory
evidence, not as a valid pose result. Although its left/right wrist inputs were
distinct, both independently solved paths inherited the same world-`y` elbow
pole sign. This made the right elbow choose the inward branch and visually
resembled copying one arm posture onto the other. Its Isaac videos must not be
used as successful pose evidence.

The corrected run is
`t007_offline_continuation_v7_elbow_branch_20260822T220000Z`. Each arm now uses
its own wrist trace and a side-aware anatomical pole convention (`+y` outward
left, `-y` outward right); no opposite-arm state appears in either arm solve.
Across 463 samples, inward-pole fractions are 2.81% left and 3.67% right.
Wrist position p95 is 54.6 mm left and 41.0 mm right. At the maximum-separation
frame, post-limit shoulder-elbow-endpoint angles are 168.2° left and 147.5°
right; the raw right anchor was 152.3°. Joint step, velocity and acceleration
limits pass at 0.14993 rad, 4.48 rad/s and 45.0 rad/s².

This run remains a declared **fail**, not a pass: right-arm post-limit
straightness misses the 150° gate, and the left maximum wrist error contains a
229 mm transient. The dependency-free bilateral solve took 189.3 s for the
15.44 s trace (0.0816× real time). It is intended for offline branch selection,
not live control.

The corresponding 0.5× Isaac replay is
`t007_offline_continuation_v7_elbow_branch_isaac_slow_20260822T223000Z`.
It completed the full command file, held the fixed root exactly, and recorded a
36 s dual-view video. Trajectory lookup cost was 0.214 ms median and 0.464 ms
p95 at 30 Hz. Source-target-to-observed-FK p95 was 93.8 mm left and 81.9 mm
right; command-FK-to-observed-FK p95 was 80.4 mm and 76.4 mm. Shoulder-pitch
tracking under gravity remains the dominant Isaac articulation error. The
video verifies the corrected outward elbow branches visually, but the numeric
failures above prevent a claim of fully accurate tracking.
